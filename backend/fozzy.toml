base_dir = ".fozzy"

[package]
name = "lifestream-backend"
version = "0.1.0"

[[target.bin]]
name = "lifestream-backend"
path = "fozzy/main.fzy"

[unsafe]
contracts = "compiler"
enforce_dev = false
enforce_verify = true
enforce_release = true
deny_unsafe_in = []
allow_unsafe_in = []
